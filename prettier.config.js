// @ts-check

// This file exists so that the Prettier extension for VS Code uses Prettier
// from the project's dependencies instead of the one bundled with the extension.
// That's why the configuration is empty.

/**
 * @see https://prettier.io/docs/en/configuration.html
 * @type {import('prettier').Options}
 * */
const config = {};

export default config;
